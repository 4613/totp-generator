# Offline TOTP Generator

A lightweight, client-side TOTP (2FA) generator that runs entirely in your browser.

No servers. No tracking. No external requests.

Just open the page, paste your Base32 key, and get rotating 6-digit codes instantly.

## Features

- Fully offline — everything runs locally in your browser
- Smooth 30-second progress bar
- Instant code generation on paste or edit
- Automatic Base32 formatting (`XXXX XXXX XXXX XXXX`)
- Input validation and length limiting
- Secret hidden by default with eye toggle
- Local persistence using browser storage
- One-click copy
- Clean, minimal UI
- Zero dependencies
